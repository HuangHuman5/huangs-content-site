Drop album cover images in this folder (e.g. `low-tide-radio.jpg`), then
reference them from `data/albums.json` like:

  "cover": "data/covers/low-tide-radio.jpg"

Leave "cover" as an empty string to fall back to the lettered placeholder.
